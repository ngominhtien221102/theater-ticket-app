Option Strict On
Option Explicit On

Namespace TheaterTicketingApp.Models
    Public Module SeatTypes
        ' Keep DB value as REGULAR for backward compatibility with existing schema/data.
        Public Const SingleType As String = "REGULAR"
    End Module
End Namespace
